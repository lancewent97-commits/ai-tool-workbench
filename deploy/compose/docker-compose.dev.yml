services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: ai_tool_workbench
      POSTGRES_USER: ai_tool_workbench
      POSTGRES_PASSWORD: change-me
    ports:
      - "127.0.0.1:5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ai_tool_workbench -d ai_tool_workbench"]
      interval: 5s
      timeout: 3s
      retries: 10

volumes:
  postgres-data:
